#!/bin/bash
#This is a file containing ALL the handlers for MindsDB, I have compiled them all and I can tell that its absolutely INSANE!
#I would rather only install what you REALLY need and install them one-by-one. 
#I have done so as part of testing and I dont intend to follow the same excercise again on production enciroment
# You can still use the list below as a reference. 


pip install mindsdb[access]
pip install mindsdb[aerospike]
pip install mindsdb[airtable]
pip install mindsdb[altibase]
pip install mindsdb[anomaly_detection]
pip install mindsdb[anthropic]
pip install mindsdb[anyscale_endpoints]
pip install mindsdb[apache_doris]
pip install mindsdb[aqicn]
pip install mindsdb[aurora]
pip install mindsdb[autogluon]
pip install mindsdb[autokeras]
pip install mindsdb[autosklearn]
pip install mindsdb[bigquery]
pip install mindsdb[binance]
pip install mindsdb[byom]
pip install mindsdb[cassandra]
pip install mindsdb[chromadb]
pip install mindsdb[ckan]
pip install mindsdb[clickhouse]
pip install mindsdb[clipdrop]
pip install mindsdb[cloud_spanner]
pip install mindsdb[cloud_sql]
pip install mindsdb[cockroach]
pip install mindsdb[cohere]
pip install mindsdb[coinbase]
pip install mindsdb[confluence]
pip install mindsdb[couchbase]
pip install mindsdb[crate]
pip install mindsdb[d0lt]
pip install mindsdb[databend]
pip install mindsdb[databricks]
pip install mindsdb[datastax]
pip install mindsdb[db2]
pip install mindsdb[derby]
pip install mindsdb[discord]
pip install mindsdb[dockerhub]
pip install mindsdb[documentdb]
pip install mindsdb[dremio]
pip install mindsdb[druid]
pip install mindsdb[duckdb]
pip install mindsdb[dummy_data]
pip install mindsdb[dynamodb]
pip install mindsdb[edgelessdb]
pip install mindsdb[elasticsearch]
pip install mindsdb[email]
pip install mindsdb[empress]
pip install mindsdb[eventbrite]
pip install mindsdb[eventstoredb]
pip install mindsdb[faunadb]
pip install mindsdb[file]
pip install mindsdb[firebird]
pip install mindsdb[flaml]
pip install mindsdb[frappe]
pip install mindsdb[github]
pip install mindsdb[gitlab]
pip install mindsdb[gmail]
pip install mindsdb[google_analytics]
pip install mindsdb[google_books]
pip install mindsdb[google_calendar]
pip install mindsdb[google_content_shopping]
pip install mindsdb[google_fit]
pip install mindsdb[google_gemini]
pip install mindsdb[google_search]
pip install mindsdb[hackernews]
pip install mindsdb[hana]
pip install mindsdb[hive]
pip install mindsdb[hsqldb]
pip install mindsdb[hubspot]
pip install mindsdb[huggingface_api]
pip install mindsdb[huggingface]
pip install mindsdb[ignite]
pip install mindsdb[impala]
pip install mindsdb[influxdb]
pip install mindsdb[informix]
pip install mindsdb[ingres]
pip install mindsdb[instatus]
pip install mindsdb[intercom]
pip install mindsdb[jira]
pip install mindsdb[kinetica]
pip install mindsdb[lancedb]
pip install mindsdb[langchain_embedding]
pip install mindsdb[langchain]
pip install mindsdb[leonardoai]
pip install mindsdb[libsql]
pip install mindsdb[lightdash]
pip install mindsdb[lightfm]
pip install mindsdb[lightwood]
pip install mindsdb[lindorm]
pip install mindsdb[litellm]
pip install mindsdb[llama_index]
pip install mindsdb[ludwig]
pip install mindsdb[luma]
pip install mindsdb[mariadb]
pip install mindsdb[materialize]
pip install mindsdb[matrixone]
pip install mindsdb[maxdb]
pip install mindsdb[mediawiki]
pip install mindsdb[mendeley]
pip install mindsdb[merlion]
pip install mindsdb[milvus]
pip install mindsdb[mlflow]
pip install mindsdb[monetdb]
pip install mindsdb[mongodb]
pip install mindsdb[monkeylearn]
pip install mindsdb[ms_teams]
pip install mindsdb[mssql]
pip install mindsdb[mysql]
pip install mindsdb[neuralforecast]
pip install mindsdb[newsapi]
pip install mindsdb[notion]
pip install mindsdb[npm]
pip install mindsdb[nuo_jdbc]
pip install mindsdb[oceanbase]
pip install mindsdb[oilpriceapi]
pip install mindsdb[ollama]
pip install mindsdb[openai]
pip install mindsdb[openbb]
pip install mindsdb[opengauss]
pip install mindsdb[openstreetmap]
pip install mindsdb[oracle]
pip install mindsdb[orioledb]
pip install mindsdb[palm]
pip install mindsdb[paypal]
pip install mindsdb[pgvector]
pip install mindsdb[phoenix]
pip install mindsdb[pinecone]
pip install mindsdb[pinot]
pip install mindsdb[pirateweather]
pip install mindsdb[plaid]
pip install mindsdb[planetscale]
pip install mindsdb[popularity_recommender]
pip install mindsdb[postgres]
pip install mindsdb[pycaret]
pip install mindsdb[pypi]
pip install mindsdb[qdrant]
pip install mindsdb[questdb]
pip install mindsdb[quickbooks]
pip install mindsdb[rag]
pip install mindsdb[ray_serve]
pip install mindsdb[reddit]
pip install mindsdb[redshift]
pip install mindsdb[replicate]
pip install mindsdb[rocket_chat]
pip install mindsdb[rockset]
pip install mindsdb[s3]
pip install mindsdb[sap_erp]
pip install mindsdb[scylla]
pip install mindsdb[sendinblue]
pip install mindsdb[sentence_transformers]
pip install mindsdb[sharepoint]
pip install mindsdb[sheets]
pip install mindsdb[shopify]
pip install mindsdb[singlestore]
pip install mindsdb[slack]
pip install mindsdb[snowflake]
pip install mindsdb[solace]
pip install mindsdb[solr]
pip install mindsdb[spacy]
pip install mindsdb[sqlany]
pip install mindsdb[sqlite]
pip install mindsdb[sqreamdb]
pip install mindsdb[stabilityai]
pip install mindsdb[starrocks]
pip install mindsdb[statsforecast]
pip install mindsdb[strapi]
pip install mindsdb[strava]
pip install mindsdb[stripe]
pip install mindsdb[supabase]
pip install mindsdb[surrealdb]
pip install mindsdb[symbl]
pip install mindsdb[tdengine]
pip install mindsdb[teradata]
pip install mindsdb[tidb]
pip install mindsdb[timegpt]
pip install mindsdb[timescaledb]
pip install mindsdb[tpot]
pip install mindsdb[trino]
pip install mindsdb[tripadvisor]
pip install mindsdb[twelve_labs]
pip install mindsdb[twilio]
pip install mindsdb[twitter]
pip install mindsdb[utilities]
pip install mindsdb[vertex]
pip install mindsdb[vertica]
pip install mindsdb[vitess]
pip install mindsdb[weaviate]
pip install mindsdb[web]
pip install mindsdb[webz]
pip install mindsdb[whatsapp]
pip install mindsdb[writer]
pip install mindsdb[xata]
pip install mindsdb[youtube]
pip install mindsdb[yugabyte]
pip install mindsdb[zipcodebase]
